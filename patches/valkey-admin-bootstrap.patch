diff --git a/apps/frontend/src/state/epics/valkeyEpics.ts b/apps/frontend/src/state/epics/valkeyEpics.ts
--- a/apps/frontend/src/state/epics/valkeyEpics.ts
+++ b/apps/frontend/src/state/epics/valkeyEpics.ts
@@ -69,6 +69,23 @@ export const connectionEpic = (store: Store) =>
       select(wsConnectFulfilled),
       take(1),
       tap(() => {
+        fetch("/bootstrap")
+          .then((response) => response.ok ? response.json() : null)
+          .then((connectionDetails) => {
+            if (!connectionDetails) return
+            store.dispatch(connectPending({
+              connectionId: buildConnectionId(
+                connectionDetails.host,
+                connectionDetails.port,
+                connectionDetails.db,
+              ),
+              connectionDetails,
+              usePreconfiguredConnection: true,
+            }))
+          })
+          .catch(() => undefined)
+
         const host = import.meta.env.VITE_LOCAL_VALKEY_HOST
         const port = import.meta.env.VITE_LOCAL_VALKEY_PORT
         const alias = import.meta.env.VITE_LOCAL_VALKEY_NAME
diff --git a/apps/server/src/actions/connection.ts b/apps/server/src/actions/connection.ts
--- a/apps/server/src/actions/connection.ts
+++ b/apps/server/src/actions/connection.ts
@@ -1,4 +1,5 @@
 import { GlideClusterClient } from "@valkey/valkey-glide"
+import { initialConnectionDetails, preConfiguredConnection } from "../metrics-orchestrator"
 import { EndpointType, toNodeId } from "valkey-common"
 import { VALKEY } from "valkey-common"
 import { connectToValkey, teardownConnection  } from "../connection"
@@ -20,6 +21,7 @@ type ConnectPayload = {
   connectionDetails: ConnectionDetails,
   connectionId: string,
+  usePreconfiguredConnection?: boolean,
   isRetry?: boolean,
   isResume?: boolean,
 }
@@ -29,7 +31,13 @@ export const connectPending = withDeps<Deps, void>(
       return
     }
 
+    const connectionDetails = payload.usePreconfiguredConnection && preConfiguredConnection
+      ? initialConnectionDetails
+      : payload.connectionDetails
+
     const client = await connectToValkey(
       { clients, connectedNodesByCluster, metricsServerMap, clusterNodesRegistry },
       ws,
-      payload,
+      { ...payload, connectionDetails },
     )
diff --git a/apps/server/src/index.ts b/apps/server/src/index.ts
--- a/apps/server/src/index.ts
+++ b/apps/server/src/index.ts
@@ -77,6 +77,18 @@ app.use(express.json())
 const metricsRouter = createMetricsOrchestratorRouter()
 app.use("/orchestrator", metricsRouter)
 
+app.get("/bootstrap", (_req: Request, res: Response) => {
+  if (!preConfiguredConnection) {
+    res.sendStatus(204)
+    return
+  }
+
+  const { password, ...connectionDetails } = initialConnectionDetails
+  res.json(connectionDetails)
+})
+
 // Fallback to index.html for SPA routing
 app.get("*", (_req: Request, res: Response) => {
   res.sendFile(path.join(frontendDist, "index.html"))
